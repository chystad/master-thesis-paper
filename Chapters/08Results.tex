\hl{Result chapter intro}


\section{Comparative Propagation Analysis Results}
This section presents the results from the comparative propagation benchmarking campaign described in Section \ref{sec:comparative_prop_anal_method}. The continuously updating Skyfield-SGP4 propagation is used as the comparison baseline, while three Basilisk configurations are evaluated against it: the primary thesis configuration with NRLMSISE-00 atmospheric density, the specialization-project configuration with an un-tuned exponential atmosphere model, and a re-tuned exponential atmosphere configuration. All reported differences are computed as Basilisk minus Skyfield-SGP4 and should be interpreted as differences relative to the updating-\gls{tle} reference, not as errors relative to a true physical ground truth.

The results are first presented using same-spacecraft state differences for the leader spacecraft, followed by an RTN decomposition of the position difference, altitude differences, ground-track offsets, and same-formation drift differences for the passive leader-follower pair. Together, these quantities describe both the Earth-relative divergence of each Basilisk configuration and the corresponding difference in predicted formation drift. This provides the propagation benchmark used to contextualize the GNSS-R formation flight lifetime analysis.

\begin{comment}
Include a short introduction briefly explaining the campaign and the differently configured simulator runs that created these results:
    * Skyfield-SGP4 with continuously updating TLEs is the reference baseline.
    * Three Basilisk configurations are compared:
        - current thesis configuration with NRLMSISE-00,
        - specialization-project exponential model,
        - retuned exponential model.
    * All differences are Basilisk minus Skyfield.
    * Remind the reader that the comparison is a benchmark against an approximate reference, not a validation against truth.
    * Refer back to the campaign table in the methodology chapter.
\end{comment}


\subsection{Same-Spacecraft State Magnitude Difference}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{Figures/05Results/bsk_skf_comparison/Ex1 - ECI pos-vel difference magnitude.png}
    \caption{Leader spacecraft ECI position and velocity difference magnitudes between various Basilisk configuration and the updating TLE Skyfield-SGP4 baseline}
    \label{fig:same_sc_ECI_state_mag_diff} 
\end{figure}

Figure \ref{fig:same_sc_ECI_state_mag_diff} shows the same-spacecraft ECI position and velocity difference magnitudes for the leader spacecraft over the 14-day propagation benchmarking campaign. The differences are computed between each Basilisk configuration and the continuously updating Skyfield-SGP4 baseline. Both the position and velocity difference magnitudes increase throughout the propagation interval for all Basilisk configurations. The ordering of the configurations is the same for both translational states: the primary thesis Basilisk configuration with the NRLMSISE-00 atmosphere model has the smallest divergence, the re-tuned exponential atmosphere configuration has an intermediate divergence, and the specialization-project exponential atmosphere configuration has the largest divergence.

At the end of the 14-day propagation interval, the primary thesis Basilisk configuration has reached a leader position difference of approximately $900~\mathrm{km}$ relative to the Skyfield-SGP4 baseline. The re-tuned exponential atmosphere configuration reaches approximately $1500~\mathrm{km}$, while the specialization-project exponential atmosphere configuration reaches approximately $2900~\mathrm{km}$. The velocity difference magnitudes show the same relative ordering. 

\begin{comment}
* Figure \ref{same_sc_ECI_pos_vel_mag_diff} shows the magnitude of the ECI position and velocity difference between various Basilisk configurations and the continuously updating TLE Skyfield-SGP4 comparison baseline over a 14 day period. 
* Both the position and velocity difference magnitudes show very similar diverging behavior, and the divergence ordering from least to most is the same for both positioning and velocity. 
* Both translational states show an increasing divergence rate with time
* After 14 days, the thesis Basilisk configuration has diverged by approximately 900km in position, the Basilisk configuration evaluated in the Specaialization project has diverged by approx 2900km and the tuned exponential atmosphere configuration has diverged by approx 1500km. 
* The same ratio of divergence is also about the same for the velocity magnitude differences, where the tuned basilisk model has diverged approx 70% more than the thesis Configuration, and the old configuration has diverged approx 240% more
\end{comment}



\subsection{Decomposition of Same-Spacecraft Position Difference}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{Figures/05Results/bsk_skf_comparison/Ex1 - RTN pos difference.png}
    \caption{Decomposition of the same-spacecraft position difference in the RTN frame}
    \label{fig:same_sc_pos_RTN_decomposition} 
\end{figure}
Figure \ref{fig:same_sc_pos_RTN_decomposition} shows the same-spacecraft position difference for the leader spacecraft decomposed in the Skyfield-SGP4 baseline leader's RTN frame. The along-track component is the dominant contributor to the position difference for all Basilisk configurations. The primary thesis configuration with the NRLMSISE-00 atmosphere model diverges in the positive along-track direction, while both exponential atmosphere configurations diverge in the negative along-track direction. By the end of the 14-day comparison interval, the old specialization-project exponential atmosphere configuration has the largest along-track difference, followed by the re-tuned exponential atmosphere configuration and the primary thesis configuration.

The cross-track separation remains small compared with the along-track and radial components throughout the comparison. The plotted cross-track component remains oscillatory and within a few kilometers for all Basilisk configurations. The radial component is also smaller than the along-track component, but shows a clear separation between the configurations. The old specialization-project exponential atmosphere configuration has the largest radial difference, while the primary thesis configuration has the smallest radial difference. The ordering of the RTN components is therefore consistent with the ECI state-difference magnitudes shown in Figure \ref{fig:same_sc_ECI_state_mag_diff}.


\begin{comment}
* Figure \ref{fig:same_sc_pos_RTN_decomposition} show how the same-spacecraft position difference magnitude presented in Figure \ref{fig:same_sc_ECI_state_mag_diff} is expressed in terms of along-track, cross-track and radial difference.
* The RTN decomposition show that the absolute difference in the in each component follow the same ordering as the magnitudes. In increasing along-track, cross-track and radial absolute difference order: Thesis configuration, tuned Basilisk, old Basilisk. 
* An interesting result is that the rate of divergence for the Old Basilisk configuration is much bigger in the radial component compared to both the along-track and cross-track rates. 
* Another interesting result is that while the old and tuned exponential atmosphere Basilisk configurations diverge in the negative along-track direction, the thesis configuration diverges in the positive direction
\end{comment}

\begin{figure}[htbp]
    \centering

    \resizebox{\textwidth}{!}{%
        \includegraphics[height=0.34\textheight]{Figures/05Results/bsk_skf_comparison/Ex1 - Groundtrack comparison.png}
        \hspace{0.03\textwidth}
        \includegraphics[height=0.34\textheight]{Figures/05Results/bsk_skf_comparison/Ex1 - Zoomed-in Groundtrack comparison.png}
    }

    \caption{Surface projections for the leader satellite position over time propagated by each Basilisk configuration and the updating TLE Skyfield-SGP4 baseline. The zoomed-out view is shown on the left, while the zoomed-in view of the north-western coast of Norway is shown on the right.}
    \label{fig:same_sc_ground_track}
\end{figure}
Figure \ref{fig:same_sc_ground_track} shows the corresponding ground-track tracings for the same leader trajectories. The left subplot shows the ground-track comparison over the Nordic countries during the final six hours of the simulation, while the right subplot shows a zoomed-in view near the north-western coast of Norway. The visible separation between the projected tracks is largest for the old specialization-project exponential atmosphere configuration and smallest for the primary thesis configuration. The estimated ground-track offset relative to the Skyfield-SGP4 baseline is approximately $17~\mathrm{km}$ for the primary thesis configuration, $32~\mathrm{km}$ for the re-tuned exponential atmosphere configuration, and $60~\mathrm{km}$ for the old specialization-project configuration.

The ground-track offset shown in Figure \ref{fig:same_sc_ground_track} is not equivalent to the cross-track separation shown in Figure \ref{fig:same_sc_pos_RTN_decomposition}. The cross-track separation is the orbit-normal component of the three-dimensional position difference in the RTN frame, while the ground-track offset is the separation between the Earth-projected spacecraft trajectories. The RTN decomposition shows that the three-dimensional position difference is dominated by along-track drift, whereas the ground-track comparison shows the surface projection of this accumulated drift onto the Earth-fixed latitude-longitude map.

\begin{comment}
* Figure \ref{fig:same_sc_ground_track} show the leader spacecraft position projected onto the Earth's surface for each propagator. 
* The ground track offset relative to the Skyfield-SGP4 baseline has been estimated to be 17km for the thesis configuration, 32km for the tuned exponential atmosphere config, and 60km for the old Basilisk configuration. 
* Point out that the ground-track offset is not the same as the cross-track difference shown in Figure \ref{fig:same_sc_pos_RTN_decomposition} because of the long radial and along-track separation between the propagated leader spacecraft
    - The ground-track comparison shows the surface projection of the accumulated propagation difference and should not be interpreted directly as an RTN component plot. Although the tracks appear laterally separated on the map, the RTN decomposition shows that the corresponding 3D spacecraft separation is dominated by along-track phase drift, while the orbit-normal component remains small. The apparent ground-track offset is therefore mainly a consequence of along-track timing differences in an inclined orbit, projected onto the Earth-fixed latitude/longitude map.
\end{comment}



\subsection{Altitude Difference Comparison}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{Figures/05Results/bsk_skf_comparison/Ex1 - Altitude difference.png}
    \caption{Leader spacecraft orbital altitudes for each Basilisk configuration compared against the updating TLE Skyfield-SGP4 baseline}
    \label{fig:same_sc_altitude_difference} 
\end{figure}

Figure \ref{fig:same_sc_altitude_difference} shows the leader spacecraft altitude difference for each Basilisk configuration relative to the continuously updating Skyfield-SGP4 baseline. All Basilisk configurations exhibit short-period oscillations in altitude difference, with amplitudes that increase over the 14-day propagation interval. The primary thesis configuration with the NRLMSISE-00 atmosphere model remains closest to the Skyfield-SGP4 baseline throughout the comparison, while the re-tuned exponential atmosphere configuration shows a larger altitude difference and the old specialization-project exponential atmosphere configuration shows the largest altitude difference.

The long-term altitude trend differs between the atmosphere models. The primary thesis configuration gradually decreases in altitude relative to the Skyfield-SGP4 baseline, while both exponential atmosphere configurations diverge in the positive altitude direction. The old specialization-project exponential atmosphere configuration has the largest positive altitude difference by the end of the comparison interval. The ordering of the altitude differences is therefore consistent with the previous same-spacecraft state difference results: the primary thesis configuration gives the smallest deviation from the Skyfield-SGP4 baseline, followed by the re-tuned exponential atmosphere configuration and then the old specialization-project configuration.


\begin{comment}
* Figure \ref{fig:same_sc_altitude_difference} shows the leader spacecraft orbital altitude difference for each Basecraft configuration compared against the Skyfield-SGP4 baseline. 
* Every Basilisk configuration exhibits short term oscillatory behavior increasing in amplitude over time. 
* The short term oscillations is a due to a compbination of the scenario's non-circular orbit, and a growing along-track difference. The oscillation period is consistent with the orbital period
* The general trend however, show that the altitude of the thesis Basilisk configuration is slightly decreasing relative to the SGP4 baseline, while the tuned and old configurations diverge in the positive altitude direction. 
* The difference ordering is again the same as previous figures have shown: thesis configuration deviate the least, followed by the tuned configuration, and lastly, the old configuration. 
\end{comment}


\subsection{Decomposition of Same-Formation Drift Difference}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{Figures/05Results/bsk_skf_comparison/Ex1 - Leader follower RTN relative position difference.png}
    \caption{Leader-follower pair same-formation position drift difference for each Basilisk configuration compared against the updating TLE Skyfield-SGP4 baseline.}
    \label{fig:same_form_drift_RTN_decomposition} 
\end{figure}

Figure \ref{fig:same_form_drift_RTN_decomposition} shows the same-formation drift difference for the leader-follower pair, decomposed in the leader RTN frame. The plotted quantities compare the leader-relative follower position predicted by each Basilisk configuration against the corresponding leader-relative follower position predicted by the continuously updating Skyfield-SGP4 baseline. All three RTN components exhibit short-period oscillations with amplitudes that increase throughout the 14-day comparison interval.

The along-track component is the dominant same-formation drift difference. All Basilisk configurations show a positive along-track drift difference relative to the Skyfield-SGP4 baseline, reaching approximately $4~\mathrm{km}$ by the end of the propagation interval. The three Basilisk configurations remain close to each other in this component. Although more oscillatory, the old specialization-project exponential atmosphere configuration shows slightly smaller final along-track drift difference than the primary thesis and re-tuned exponential configurations, although it also exhibits stronger short-period oscillations.

The cross-track and radial components remain much smaller than the along-track component. In these components, the same ordering observed in the same-spacecraft state difference results is present: the primary thesis configuration has the smallest divergence, the re-tuned exponential atmosphere configuration is intermediate, and the old specialization-project exponential atmosphere configuration has the largest oscillation amplitude. The radial drift difference remains below approximately $150~\mathrm{m}$, while the cross-track drift difference remains within approximately $\pm 10~\mathrm{m}$ over the full comparison interval.

\begin{comment}
Just like the altitude comparison, every component of the RTN position drift decomposition is subject to short-term oscillations with growing amplitude. For the cross-track and radial components, the usual difference ordering is present: The thesis configuration diverges the least, followed by the tuned and old configurations in ascending order. However, the main thing to point is that the Old configuration, though more oscillatory,  diverges ever so slightly less than the other configurations.
\end{comment}







% ======================================================================================================================================= %
% ======================= G N S S - R    F O R M A T I O N    F L I G H T    S I M U L A T O R    R E S U L T S ======================= %
% ======================================================================================================================================= %

\clearpage
\section{Deployment Velocity Acquisition Fuel Sensitivity}
\label{sec:deployment_vel_acq_fuel_sens}
This section presents the results from the deployment velocity fuel sensitivity campaign described in Section \ref{sec:deploy_sensitivity_campaign}. The campaign consists of nine 48-hour GNSS-R formation flight simulations where one leader and one follower are initialized from the same deployer orbit, but with different opposite deployment velocity offsets. The differential deployment speed $\Delta v_{\mathrm{dep}}$ is varied from $0$ to $4~\mathrm{m/s}$. For each case, the acquisition time $t_{\mathrm{acq}}$ is determined using the \gls{oed} acquisition criterion defined in Section \ref{sec:formation_acquisition_def}, and the corresponding acquisition fuel consumption $m_{\mathrm{acq}}$ is computed as the follower fuel consumed from deployment until this acquisition time.

The results are first presented using leader-follower RTN relative-position trajectories to show the geometric formation response during and after the initial deployment transient. The acquisition condition is then evaluated using the \gls{oed} tolerances that define $t_{\mathrm{acq}}$, before the follower fuel consumption is used to extract $m_{\mathrm{acq}}$ for each deployment case. The section concludes by summarizing the resulting acquisition time, acquisition fuel, burn statistics, and operational diagnostics. Together, these results provide the simulation-based deployment sensitivity estimate of $m_{\mathrm{acq}}(\Delta v_{\mathrm{dep}})$ used in the GNSS-R formation flight lifetime analysis.

\begin{comment}
Insert short introduction here including
* Nine 48-hour simulations.
* One leader and one follower initialized from the shared deployer.
* Differential deployment speed $\Delta v_{\mathrm{dep}}$ varied from $0$ to $4~\mathrm{m/s}$.
* The acquisition condition from the methodology is used to determine $t_{\mathrm{acq}}$.
* The follower fuel consumed up to $t_{\mathrm{acq}}$ defines $m_{\mathrm{acq}}$.
\end{comment}


\subsection{RTN Formation Trajectories}

\begin{figure}[htbp]
    \centering

    \begin{minipage}[c]{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{Figures/05Results/deployment_sensitivity_analysis/deployment_velocity_rtn_relative_position_3d_selected_runs_0_15h.png}
    \end{minipage}
    \hfill
    \begin{minipage}[c]{0.49\textwidth}
        \centering
        \includegraphics[width=\linewidth]{Figures/05Results/deployment_sensitivity_analysis/deployment_velocity_rtn_relative_position_3d_selected_runs_15_48h.png}
    \end{minipage}

    \caption{Leader-follower RTN relative-position trajectories for a selected set of differential deployment velocities corresponding to run 0, 2, 4, 6 and 8 detailed in \ref{sec:deploy_sensitivity_campaign}. The left plot shows the initial transient response over $t \in [0, 15]$ h, while the right plot shows the subsequent bounded relative motion over $t \in [15, 48]$ h.}
    \label{fig:diff_deploy_rtn_relative_position_3d_trajectories}
\end{figure}


\clearpage
Figure \ref{fig:diff_deploy_rtn_relative_position_3d_trajectories} shows the leader-follower relative-position trajectories for selected deployment velocity cases, expressed in the leader RTN frame. The left plot shows the initial response during the first $15 \mathrm{h}$ after deployment, while the right plot shows the relative motion over the remaining interval from $15$ to $48 \mathrm{h}$. The largest initial relative-position magnitude occur for the largest differential deployment speeds, but all cases evolve into similar bounded relative-position trajectories.

\begin{comment}
* Figure \ref{fig:diff_deploy_rtn_relative_position_3d_trajectories} show the formation trajectories for a selected number of runs (differential deployment speeds 0, 1, 2, 3 and 4m/s) expressed in the \gls{rtn} frame while influenced by the formation control system. The left plot show the transient formation acquisition for times up until 15 hours after deployment, while the right plot show the more stable formation after the initial transients have faded. (be very careful not to use the word "acquired" here, because the acquisition time will only be determined in the next subsection)
* The RTN trajectories are decomposed into its Radial, Along-track and Cross-track components in Figure \ref{fig:diff_deploy_rtn_relative_position_decomposition}. The vertical dashed lines show acquisition times of the runs of same color, but this will be presented in more detail in the next subsections.  
* Comment on the observed results in these plots:
    * The general trend seem to be that the transient oscillatory position difference increase in magnitude with increased differential deployment velocity. This is especially apparent in the cross-track component in Figure \ref{diff_deploy_rtn_relative_position_decomposition}, and the transient trajectories
    * Despite this, the 
Point out that, while it is tempting to attribute the large initial follower deviation from the desired formation geometry purely to its differential deployment speed, it is also because of formation acquisition burns. 
\end{comment} 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Figures/05Results/deployment_sensitivity_analysis/deployment_velocity_rtn_relative_position_components_selected_runs.png}
    \caption{Decomposition of the leader-follower RTN relative-position trajectories into its individual components over $t \in [0, 15]$. Only a selected set of differential deployment velocities are included, corresponding to run 0, 2, 4, 6 and 8 detailed in \ref{sec:deploy_sensitivity_campaign}. The vertical dashed lines show the corresponding color's acquisition time. }
    \label{fig:diff_deploy_rtn_relative_position_decomposition} 
\end{figure}

The component-wise RTN relative-position time series is shown in Figure \ref{fig:diff_deploy_rtn_relative_position_decomposition}. The radial, along-track, and cross-track components all show transient oscillations during the initial formation response. The largest transient amplitudes occur for the highest differential deployment speed cases, most clearly in the cross-track component. The $\Delta v_{\mathrm{dep}} = 4~\mathrm{m/s}$ case reaches the largest along-track displacement, while the lower deployment speed cases remain closer to the leader during the initial interval. The vertical dashed lines in Figure \ref{fig:diff_deploy_rtn_relative_position_decomposition} indicate the acquisition times determined for the corresponding deployment velocity cases. These acquisition times are evaluated formally in the next subsection using the orbital-element-difference acquisition criterion. The RTN trajectories shown here therefore present the geometric leader-follower response before and after the initial formation insertion process.









\subsection{Formation Acquisition Determination}
The formation acquisition time was determined from the time evolution of the follower-leader classical orbital element differences, shown in Figure \ref{fig:diff_deploy_oed} for a selected subset of all cases presented in Section \ref{sec:deploy_sensitivity_campaign}. For this campaign, acquisition was defined by the follower remaining within the specified tolerance bounds for the selected acquisition-relevant \gls{oed} components: $\Delta a/a$, $\Delta e$, and $\Delta i$, in accordance with Section \ref{sec:formation_acquisition_def}. The tolerance ranges are shown as black dashed horizontal lines, and the vertical dashed lines show the corresponding acquisition times.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Figures/05Results/deployment_sensitivity_analysis/deployment_velocity_oed_da_de_di_comparison.png}
    \caption{Classic orbital element differences between the leader-follower pair for a selected set of differential deployment velocities corresponding to run 0, 2, 4, 6 and 8 detailed in \ref{sec:deploy_sensitivity_campaign}. The horizontal dashed lines show the OED tolerance range, while the vertical dashed lines show the acquisition times. The plotted time-horizon is reduced to $t \in [0, 15]$.}
    \label{fig:diff_deploy_oed} 
\end{figure}

The results show that the initial differential deployment speeds produces different transient \gls{oed} responses that eventually converge within the formation tolerances at different rates. The steep changes observed in the \gls{oed} histories are caused by formation-control burn maneuvers, which rapidly adjust the relative orbital elements. These jumps are especially visible in $\Delta a/a$ and $\Delta i$, where the commanded burns rapidly changes the semi-major axis and inclination difference. Between these burn events, the \gls{oed}s evolve more smoothly due to the natural relative orbital motion and perturbation environment.

% The acquisition time generally increases with differential deployment speed, but the trend is not strictly true for the displayed cases. The largest differential deployment speed case is acquired slightly before the next-highest case, which is the only visible deviation from the overall trend of the displayed cases. The figure therefore indicates that increasing differential deployment speed generally increases the time required to establish the target formation, while the exact acquisition time also depends on the timing and effect of the discrete formation-control burn sequence. The exact acquisition times for all differential deployment speeds considered in this campaign are presented in Figure \ref{fig:diff_deploy_curve_fitting} in the next subsection.









\subsection{Acquisition Time \& Fuel Consumption}
\label{sec:acq_time_and_fuel_consumption}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Figures/05Results/deployment_sensitivity_analysis/deployment_velocity_follower_cumulative_fuel_consumption.png}
    \caption{Cumulative formation acquisition fuel consumption for all campaign cases over $t \in [0, 15]$. The vertical dashed lines show the acquisition times.}
    \label{fig:diff_deploy_cum_fuel_consumption} 
\end{figure}

The cumulative follower fuel consumption during the first 15 hours of the deployment sensitivity campaign is shown in Figure \ref{fig:diff_deploy_cum_fuel_consumption}. The colored curves show the accumulated fuel consumed by the follower spacecraft, while the vertical dashed lines mark the acquisition time for the corresponding differential deployment speed case. The circular markers at the intersection between these lines show the acquisition fuel consumption $m_{\mathrm{acq}}$.

The fuel time series show a clear difference between the acquisition phase and the subsequent formation-keeping phase, where the general cumulative fuel consumption rate is significantly larger during acquisition. This behavior is observed across the deployment speed cases, although the number, timing, and magnitude of the burn events vary between cases. The acquisition fuel consumption also increases with differential deployment speed. The lowest deployment speed case requires only a few grams of fuel before acquisition, while the largest differential deployment speed case requires approximately 77 g. The cumulative fuel curves therefore show that larger initial deployment velocity differences lead to a larger propellant cost before the formation is acquired. This trend is more consistent for fuel consumption than for acquisition time, as each increase in differential deployment speed generally results in a larger value of $m_{\mathrm{acq}}$.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{Figures/05Results/deployment_sensitivity_analysis/deployment_velocity_acquisition_time_and_fuel_vs_deployment_speed.png}
    \caption{Formation acquisition time and cumulative fuel consumption as a function of all campaign differential deployment speed cases}.
    \label{fig:diff_deploy_curve_fitting} 
\end{figure}

Figure \ref{fig:diff_deploy_curve_fitting} summarizes the acquisition time and acquisition fuel consumption as functions of the differential deployment speed. The observed acquisition time $t_{\mathrm{acq}}$ is plotted on the left axis, while the acquisition fuel consumption $m_{\mathrm{acq}}$ is plotted on the right axis. Both quantities show a strong positive correlation with differential deployment speed. The acquisition time generally increases as $\Delta v_{\mathrm{dep}}$ increases, but the trend does not strictly apply to all cases. In particular, some intermediate cases deviate from the otherwise increasing trend, showing that the exact acquisition time depends on the timing of the discrete burn sequence and on when the dwell-time acquisition criterion is first satisfied.

A first-order prediction model was estimated for both quantities using a linear least-squares fit. The resulting approximations are shown as dashed lines in Figure \ref{fig:diff_deploy_curve_fitting}. For the acquisition time, the fitted model is

\begin{equation}
\hat{t}_{\mathrm{acq}}(\Delta v_{\mathrm{dep}}) =
0.982 \Delta v_{\mathrm{dep}} + 1.696 ,
\end{equation}

where $\hat{t}_{\mathrm{acq}}$ is given in hours when $\Delta v_{\mathrm{dep}}$ is given in $m/s$. For the acquisition fuel consumption, the fitted model is

\begin{equation}
\hat{m}_{\mathrm{acq}}(\Delta v_{\mathrm{dep}}) =
18.782 \Delta v_{\mathrm{dep}} - 2.557 ,
\end{equation}

where $\hat{m}_{\mathrm{acq}}$ is given in grams. The fuel-consumption fit follows the observed data closely, reflecting the approximately linear increase in acquisition fuel cost over the simulated deployment speed range. The acquisition-time fit captures the overall positive trend, but the observed acquisition times show larger deviations from a purely linear response. This is consistent with the stepwise nature of the formation-control process, where acquisition depends not only on the size of the initial deployment velocity difference, but also on the timing of the burn maneuvers and the dwell-time condition used to define successful acquisition.


\begin{comment}
Addressing the cumulative fuel consumption figure:
* Figure \ref{fig:diff_deploy_cum_fuel_consumption} show the cumulative fuel consumption for all differential deployment speeds considered in the campaign. The dashed vertical lines are again there to show the acquisition time for each same-colored differential deployment speed case. 
* The acquisition time generally increases with differential deployment speed, but the trend is not strictly true for all cases. For example, the largest differential deployment speed case is acquired slightly before the \Delta v_{dep}=3m/s case, and the next-largest differential deployment speed formation is acquired long before some of its smaller deployment cases. The figure therefore indicates that increasing differential deployment speed generally increases the time required to establish the target formation, while the exact acquisition time also depends on the timing and effect of the discrete formation-control burn sequence. 
* There also seems to be a more strict trend where increasing the differential deployment speed increases the fuel consumed before the acquisition time.
* In each deployment case, there is a distinct difference between the fuel consumption rate before and after the acquisition time. Before this time, the fuel consumption is steep, but "levels out" right after. 
    (This might be too discussion related): This behavior is consistent with the acquisition definition, where a larger amount of fuel is required to obtain the desired formation, followed by a decreased fuel requirement for formation maintenance after this period.

Addressing the formation-acquisition differential deployment speed sensitivity figure: 
* Figure \ref{fig:diff_deploy_curve_fitting} shows the observed acquisition time $t_{\mathrm{acq}}$ and cumulative acquisition fuel consumption $m_{\mathrm{acq}$ plotted as a function of each case's differential deployment speed. Additionally, an estimated linear model for each of these functions are shown in the figure as dashed lines.
* The linear approximations are estimated to be:
\begin{equation}
    \tilde{t_{\mathrm{acq}}$ = 0.981666667 {v_\mathrm{acq}} + 1.695925926
\end{equation}
\begin{equation}
    \tilde{m_{\mathrm{acq}}$ = 18.78246268 {v_\mathrm{acq}} - 2.567488617
\end{equation}
* where $\tilde{t_{\mathrm{acq}}$ and $\tilde{m_{\mathrm{acq}}$ is the linear approximation given in hours and m/s, respectively. 
* As observed in the cumulative fuel consumption figure, there is a strong positive correlation between both the acqusition time and fuel consumption and the differential deployment speed
\end{comment}


\subsection{Deployment Sensitivity Summary}

The differential deployment speed formation acquisition results are summarized in Table \ref{tab:deployment_velocity_acquisition_summary}. All nine deployment cases successfully acquired the desired formation within the 48-hour simulation interval. The acquisition time increased from $1.55~\mathrm{h}$ for the $\Delta v_{\mathrm{dep}} = 0.0~\mathrm{m/s}$ and $0.5~\mathrm{m/s}$ cases to $5.43~\mathrm{h}$ for the $\Delta v_{\mathrm{dep}} = 4.0~\mathrm{m/s}$ case. The acquisition fuel consumption increased from $5.60~\mathrm{g}$ for the zero-deployment-speed case to $77.02~\mathrm{g}$ for the largest deployment-speed case. The total follower fuel consumption after 48 hours follows the same overall trend, increasing from $10.42~\mathrm{g}$ to $83.41~\mathrm{g}$ across the tested deployment-speed range.

\begin{table}[H]
    \centering
    \caption{Summary of deployment velocity formation acquisition results.}
    \label{tab:deployment_velocity_acquisition_summary}
    \begin{tabular}{lccccccc}
        \toprule
        \textbf{Run} &
        $\boldsymbol{\Delta v_{\mathrm{dep}}}$ \textbf{[m/s]} &
        \textbf{Acquired} &
        $\boldsymbol{t_{\mathrm{acq}}}$ \textbf{[h]} &
        $\boldsymbol{m_{\mathrm{acq}}}$ \textbf{[g]} &
        \textbf{Burns} &
        $\boldsymbol{\bar{t}_{\mathrm{burn}}}$ \textbf{[s]} &
        $\boldsymbol{m_{\mathrm{48h}}}$ \textbf{[g]} \\
        \midrule
        0 & 0.0 & Yes & 1.55 & 5.60  & 3  & 2.27 & 10.42 \\
        1 & 0.5 & Yes & 1.55 & 5.39  & 2  & 3.20 & 12.34 \\
        2 & 1.0 & Yes & 2.50 & 10.83 & 3  & 4.33 & 21.58 \\
        3 & 1.5 & Yes & 3.88 & 24.96 & 7  & 4.27 & 33.24 \\
        4 & 2.0 & Yes & 3.88 & 29.16 & 7  & 5.23 & 34.77 \\
        5 & 2.5 & Yes & 4.73 & 43.05 & 9  & 5.74 & 49.87 \\
        6 & 3.0 & Yes & 5.48 & 59.36 & 10 & 7.09 & 66.01 \\
        7 & 3.5 & Yes & 3.92 & 59.61 & 9  & 8.26 & 67.98 \\
        8 & 4.0 & Yes & 5.43 & 77.02 & 11 & 8.37 & 83.41 \\
        \bottomrule
    \end{tabular}
\end{table}

The burn statistics in Table \ref{tab:deployment_velocity_acquisition_summary} also show that the increased acquisition fuel consumption is accompanied by both an increased number of formation-control burns and longer average thruster on-times. The number of burns during acquisition increases from $3$ in the zero-deployment-speed case to $11$ in the $\Delta v_{\mathrm{dep}} = 4.0~\mathrm{m/s}$ case. The average thruster on-time during acquisition increases from $2.27~\mathrm{s}$ to $8.37~\mathrm{s}$ over the same range.


\begin{table}[H]
    \centering
    \caption{Additional deployment acquisition diagnostics.}
    \label{tab:deployment_velocity_acquisition_diagnostics}
    \begin{tabular}{lccccc}
        \toprule
        \textbf{Run} &
        $\boldsymbol{\Delta v_{\mathrm{dep}}}$ \textbf{[m/s]} &
        $\boldsymbol{\bar{\sigma}_{\mathrm{err,burn}}}$ \textbf{[deg]} &
        $\boldsymbol{e_{\mathrm{batt,min}} }$ \textbf{[\%]} &
        \textbf{Burns denied} \\
        \midrule
        0 & 0.0 & 26.55 & 37.90 & No \\
        1 & 0.5 & 10.17 & 37.18 & No  \\
        2 & 1.0 & 18.53 & 37.43 & No \\
        3 & 1.5 & 18.61 & 37.82 & No \\
        4 & 2.0 & 12.08 & 37.18 & No \\
        5 & 2.5 & 13.31 & 37.01 & No  \\
        6 & 3.0 & 11.06 & 37.57 & No \\
        7 & 3.5 & 11.39 & 37.32 & No \\
        8 & 4.0 & 13.93 & 37.50 & No \\
        \bottomrule
    \end{tabular}
\end{table}









The additional diagnostics in Table \ref{tab:deployment_velocity_acquisition_diagnostics} show that no burn requests were denied in any of the deployment cases. The average attitude tracking error during burns remains case-dependent, but no case is marked as burn-limited by the internal subsystem logic. The follower EPS overview shown in Appendix \ref{fig:diff_deploy_eps_overview} shows the battery-energy history and instantaneous net power for all deployment cases during the acquisition interval. The operational-mode in Appendix \ref{fig:diff_deploy_operational_mode} histories show the transitions between burn, capture, charge, communication, and coast modes, while the burn diagnostics in Appendix \ref{fig:diff_deploy_burn_statistics} summarize the burn execution statistics. Across the tested deployment-speed cases, the operational constraints did not prevent the execution of any commanded formation-maintenance burns. Since entering emergency mode is the condition required to block a burn request, this indicates that the simulated battery state remained sufficient for the formation acquisition sequence in all cases.




\section{Steady-State Fuel Consumption}
This section presents the results from the formation steady-state fuel consumption campaign described in Section \ref{sec_steady_state_campaign}. The campaign uses the GNSS-R Formation Flight Simulator to propagate the selected leader-follower concentric formation after deployment, with formation control, attitude control, operational mode switching, EPS modelling, and the full thesis disturbance environment enabled. The purpose of the campaign is to estimate the cumulative post-acquisition formation-maintenance fuel consumption $m_{\mathrm{maint}}(t)$ from the follower fuel history.

The results are first presented using the follower cumulative fuel consumption over the available long-horizon simulation interval, together with fitted continuous models for $m_{\mathrm{maint}}(t)$. The section then summarizes the operational feasibility of the run using EPS, mode, pointing, and burn-execution metrics for both spacecraft. Together, these results provide the steady-state fuel-consumption model and subsystem feasibility check used in the GNSS-R formation flight lifetime estimate.

\begin{comment}
Plots:
1. Cumulative post-acquisition fuel consumption with fitted model
2. RTN states over the entire duration
3. operational mode timeline
4. Battery stored energy over tiime
5. Time spent in each operational mode


\end{comment}


\subsection{Post-Acquisition Fuel Consumption}
\label{sec:post_acq_fuel_consumption}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Figures/05Results/steady_state_fuel_rate_analysis/steady_state_fuel_consumption.png}
    \caption{Cumulative fuel consumption for a zero differential deployment speed case over 24 days. A quadratic and linear model has been fitted to the cumulative fuel consumption using samples in the timespan $t \in \left[ t_{\mathrm{acq}}, t_{\mathrm{end}} \right]$}
    \label{fig:steady_state_cum_consumption} 
\end{figure}

Figure \ref{fig:steady_state_cum_consumption} shows the cumulative follower fuel consumption from the long-horizon steady-state fuel consumption campaign. The follower is initialized with zero relative deployment velocity with respect to the leader, and the vertical dashed line marks the acquisition time $t_{\mathrm{acq}} = 1.55~\mathrm{h}$. After $24$ days of simulation time, the follower has consumed approximately $44~\mathrm{g}$ of propellant during acquisition and subsequent formation maintenance.

Two continuous models are fitted to the cumulative fuel-consumption history in the period after the acquisition time. The quadratic fit is
\begin{equation}
\hat{m}_{\mathrm{maint}}(t)
=
-0.018 t^2
+
1.979 t
+
6.683,
\end{equation}
while the linear fit is
\begin{equation}
\hat{m}_{\mathrm{maint}}(t)
=
1.536 t
+
8.471.
\end{equation}
Here, $t$ is measured in days and the fitted fuel consumption is given in grams. The fitted curves provide the simulation-based approximation of the cumulative post-acquisition formation-maintenance fuel consumption used in the lifetime estimate.


\begin{comment}
* Figure \ref{steady_state_cum_consumption} show the long-horizon cumulative fuel consumption for the follower spacecraft, which has been deployed with 0m/s relative to the leader spacecrft.
* After the full duration of 24 days, approximately 42h of fuel has been spent during acquisition and the following steady-state formation maintanance. 
* 2 models have been fitted to match the cumulative fuel consumption, one linear and one quadratic. The quadratic approximarion is given by:
\begin{equation}
    \hat{m}_{\mathrm{maint}}(t) = -0.018 t^2 + 1.979 t + 6.683
\end{equation}
And the linear approximation is given by
\begin{equation}
    \hat{m}_{\mathrm{maint}}(t) = 1.536 t + 8.471
\end{equation}
\end{comment}




\subsection{Steady-State Operational Feasibility}


Table \ref{tab:steady_state_feasibility_summary} summarizes the operational feasibility metrics from the steady-state fuel consumption campaign. The simulation was propagated for $24$ days for both spacecraft. The leader and follower show nearly identical EPS statistics, with a minimum battery energy of approximately $36\%$, a mean battery energy of approximately $42\%$, and a maximum battery energy of approximately $80\%$. The mean net power is close to zero for both spacecraft, while the instantaneous net power varies between approximately $-28~\mathrm{W}$ and $20~\mathrm{W}$. Both spacecraft spend most of the simulation time in CAPTURE mode, followed by CHARGE, COAST, and COMMS. No burn requests were denied for either spacecraft during the campaign.

\begin{table}[H]
\centering
\caption{Steady-state campaign operational feasibility metrics}
\label{tab:steady_state_feasibility_summary}
\begin{tabular}{lcc}
\toprule
\textbf{Metric} & \textbf{Leader} & \textbf{Follower} \\
\midrule

Simulation duration [days] & 24.00 & 24.00 \\
Minimum battery energy [\%] & 36.16 & 36.14 \\
Mean battery energy [\%] & 42.48 & 42.48 \\
Maximum battery energy [\%] & 79.89 & 79.89 \\
Mean net power [W] & -0.06 & -0.06 \\
Minimum net power [W] & -23.08 & -28.12 \\
Maximum net power [W] & 18.25 & 20.41 \\
Time in BURN mode [h] & 0.00 (0.00\%) & 0.02 (0.00\%) \\
Time in CAPTURE mode [h] & 328.35 (57.01\%) & 328.17 (56.97\%) \\
Time in CHARGE mode [h] & 161.63 (28.06\%) & 161.73 (28.08\%) \\
Time in COMMS mode [h] & 35.90 (6.23\%) & 35.87 (6.23\%) \\
Time in COAST mode [h] & 50.12 (8.70\%) & 50.21 (8.72\%) \\
Burns & 0 & 49 \\
Mean burn duration [s] & 0 & 1.28 \\
Mean pointing error [deg] & 0.04 & 0.36 \\
Burn denied & No & No \\
\bottomrule
\end{tabular}
\end{table}

Figure \ref{fig:steady_state_eps_modes_final_day} shows the EPS and operational-mode behaviour during the final $24~\mathrm{h}$ of the long-horizon simulation. The leader and follower battery-energy time series, net-power profiles, and operational-mode transitions are nearly identical over this interval. Both spacecraft exhibit a repeated mode sequence dominated by CAPTURE and CHARGE, with shorter intervals in COMMS and COAST. A single follower BURN-mode event occurs during the displayed interval, while the leader remains without burn activity. The similarity between the leader and follower EPS time series and mode transitions indicates that the two spacecraft remain synchronized in their steady-state operational cycle at the end of the simulation.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Figures/05Results/steady_state_fuel_rate_analysis/simple_steady_state_eps_and_pointing_mode.png}
    \caption{Operational mode transitions and EPS parameters for the leader and follower spacecraft during the final hours of the steady-state campaign}
    \label{fig:steady_state_eps_modes_final_day} 
\end{figure}




\begin{comment}
* Table \ref{tab:steady_state_feasibility_summary} show different metrics summarizing the operational states, eps and pointing behaviour during the steady-state formation maintanence mission phase. 
* The subsystem hevaviour between the leader follower are generally very similar:
    - approximately the same battery states 
    - Approximately the same time spent in the different operational states
    - approximately the same pointing error
* The only notable difference is:
    - Leader complete no burns (as per design)
    - 
\end{comment}



\section{GNSS-R Formation Flight Lifetime Estimation}

The fuel-limited lifetime estimate is obtained by combining the acquisition fuel model from Section \ref{sec:acq_time_and_fuel_consumption} with the post-acquisition maintenance fuel model from Section \ref{sec:post_acq_fuel_consumption}. The remaining follower fuel mass is modelled as
\begin{equation}
\hat{m}_{\mathrm{fuel}}(t, \Delta v_{\mathrm{dep}})
=
m_{\mathrm{fuel},0}
-
\hat{m}_{\mathrm{acq}}(\Delta v_{\mathrm{dep}})
-
\hat{m}_{\mathrm{maint}}(t),
\end{equation}
where $m_{\mathrm{fuel},0}=635~\mathrm{g}$ is the initial follower fuel mass. The fitted acquisition fuel model is
\begin{equation}
\hat{m}_{\mathrm{acq}}(\Delta v_{\mathrm{dep}})
=
18.782\Delta v_{\mathrm{dep}} - 2.557,
\end{equation}
with $\Delta v_{\mathrm{dep}}$ given in $\mathrm{m/s}$ and $\hat{m}_{\mathrm{acq}}$ in grams. The post-acquisition fuel consumption was fitted using both a quadratic and a linear model. The quadratic model is
\begin{equation}
\hat{m}_{\mathrm{maint,q}}(t)
=
-0.018t^2
+
1.979t
+
6.683,
\end{equation}
while the linear model is
\begin{equation}
\hat{m}_{\mathrm{maint,l}}(t)
=
1.536t
+
8.471.
\end{equation}
Here, $t$ is measured in days and $\hat{m}_{\mathrm{maint}}$ is given in grams. Substituting the two maintenance models into the remaining fuel expression gives
\begin{equation}
    m_{\mathrm{fuel}} \approx \hat{m}_{fuel} = \begin{cases}
        \hat{m}_{\mathrm{fuel,q}}(t, \Delta v_{\mathrm{dep}}) = 0.018t^2 - 1.979t - 18.782\Delta v_{\mathrm{dep}} + 630.874 \\
        \;\; \text{or} \\
        \hat{m}_{\mathrm{fuel,l}}(t, \Delta v_{\mathrm{dep}}) = -1.536t - 18.782\Delta v_{\mathrm{dep}} + 629.086.
    \end{cases}
\end{equation}

% \begin{equation}
% \hat{m}_{\mathrm{fuel,q}}(t, \Delta v_{\mathrm{dep}})
% =
% 0.018t^2
% -
% 1.979t
% -
% 18.782\Delta v_{\mathrm{dep}}
% +
% 630.874,
% \end{equation}
% and
% \begin{equation}
% \hat{m}_{\mathrm{fuel,l}}(t, \Delta v_{\mathrm{dep}})
% =
% -1.536t
% -
% 18.782\Delta v_{\mathrm{dep}}
% +
% 629.086.
% \end{equation}

The predicted fuel-limited lifetime is evaluated using both remaining-fuel models and the reserve condition $m_{\mathrm{res}}=0$. The quadratic lifetime estimate $\hat{T}_{\mathrm{life,q}}$ is defined as the time at which $\hat{m}_{\mathrm{fuel,q}}(t,\Delta v_{\mathrm{dep}})$ reaches zero, while the linear lifetime estimate $\hat{T}_{\mathrm{life,l}}$ is defined as the time at which $\hat{m}_{\mathrm{fuel,l}}(t,\Delta v_{\mathrm{dep}})$ reaches zero. Table \ref{tab:gnssr_lifetime_prediction_summary} summarizes the resulting lifetime estimates for the deployment speeds considered in the acquisition fuel sensitivity campaign, together with the corresponding simulated and estimated acquisition fuel consumption.
\begin{table}[H]
\centering
\caption{Predicted GNSS-R formation flight lifetime as a function of differential deployment speed using the quadratic and linear remaining fuel models.}
\label{tab:gnssr_lifetime_prediction_summary}
\begin{tabular}{ccccc}
\toprule
$\boldsymbol{\Delta v_{\mathrm{dep}}}$ \textbf{[m/s]} &
$\boldsymbol{m_{\mathrm{acq}}}$ \textbf{[g]} &
$\boldsymbol{\hat{m}_{\mathrm{acq}}}$ \textbf{[g]} &
$\boldsymbol{\hat{T}_{\mathrm{life,q}}}$ \textbf{[days]} &
$\boldsymbol{\hat{T}_{\mathrm{life,l}}}$ \textbf{[days]} \\
\midrule
0.0 & 5.60  & -2.56 & -- & 413.58 \\
0.5 & 5.39  & 6.83 & -- & 409.14 \\
1.0 & 10.83 & 16.23 & -- & 403.06 \\
1.5 & 24.96 & 25.62 & -- & 397.01 \\
2.0 & 29.16 & 35.01 & -- & 390.89 \\
2.5 & 43.05 & 44.40 & -- & 384.81 \\
3.0 & 59.36 & 53.79 & -- & 378.73 \\
3.5 & 59.61 & 63.18 & -- & 372.55 \\
4.0 & 77.02 & 72.57 & -- & 366.50 \\
\bottomrule
\end{tabular}
\end{table}

The quadratic model does not produce a finite lifetime estimate for any of the deployment-speed cases. This is indicated by the undefined entries for $\hat{T}_{\mathrm{life,q}}$ in Table \ref{tab:gnssr_lifetime_prediction_summary}. The undefined values occur because the fitted quadratic remaining-fuel model does not reach zero fuel. Instead, the positive quadratic term causes the remaining-fuel estimate to eventually increase after reaching a minimum. The linear model therefore provides the finite lifetime estimates reported in the final column of the table.
\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\textwidth]{Figures/05Results/steady_state_fuel_rate_analysis/model_predicted_lifetime.png}
    \caption{Linear remaining fuel model applied to each differential deployment speed case considered in the deployment sensitivity campaign, plotted for the estimated lifetime duration. The quadratic remaining fuel model is only applied to the zero-deployment-speed case}
    \label{fig:predicted_lifetime} 
\end{figure}
Figure \ref{fig:predicted_lifetime} shows the predicted remaining follower fuel mass over the extrapolated lifetime interval for each differential deployment speed. The dashed curves correspond to the linear remaining-fuel model and reach zero fuel at the predicted linear lifetime $\hat{T}_{\mathrm{life,l}}$. The vertical markers indicate the corresponding depletion times, which decrease from $413.58~\mathrm{days}$ for the zero-deployment-speed case to $366.50~\mathrm{days}$ for the $\Delta v_{\mathrm{dep}}=4.0~\mathrm{m/s}$ case. The quadratic model is shown for the $\Delta v_{\mathrm{dep}}=0~\mathrm{m/s}$ case and illustrates why $\hat{T}_{\mathrm{life,q}}$ is undefined.

\begin{figure}[htbp]
    \centering

    \resizebox{\textwidth}{!}{%
        \includegraphics[height=0.34\textheight]{Figures/05Results/steady_state_fuel_rate_analysis/predicted_vs_simulated_acquisition_consumption.png}
        \hspace{0.03\textwidth}
        \includegraphics[height=0.34\textheight]{Figures/05Results/steady_state_fuel_rate_analysis/predicted_vs_simulated_fuel_consumption.png}
    }

    \caption{Comparison between the linear remaining fuel model and the simulated differential deployment cases in Section \ref{sec:deployment_vel_acq_fuel_sens}. The left plot highlights fuel acquisition estimate accuracy, while the right plot shows the remaining fuel model compared to the full $48~h$ simulation duration.}
    \label{fig:predicted_vs_simulated_remaining_fuel_comparison}
\end{figure}

Figure \ref{fig:predicted_vs_simulated_remaining_fuel_comparison} compares the predicted remaining fuel time seris against the simulated fuel histories during the acquisition interval. The solid curves show the simulated follower fuel mass from the deployment velocity sensitivity campaign, while the dashed curves show the model prediction using the fitted acquisition fuel model and the linear post-acquisition fuel model. The zoomed view highlights the deployment-speed-dependent fuel offset introduced by $\hat{m}_{\mathrm{acq}}(\Delta v_{\mathrm{dep}})$ during the first part of the simulation.


\begin{comment}
* Combining the approximated models presented in Sections \ref{sec:acq_time_and_fuel_consumption} and \ref{post_acq_fuel_consumption} gives the final expression for the remaining fuel using the quadratic model:
\begin{equation}
\begin{split}
    \hat{m}_{\mathrm{fuel}}(t, \Delta v_{\mathrm{dep}}) &= m_{\mathrm{fuel},0} - m_{\mathrm{acq}}(\Delta v_{\mathrm{dep}}) - m_{\mathrm{maint}}(t)\\
    &= 635 - 18.782 \Delta v_{\mathrm{dep}} + 2.557 + 0.018 t^2- 1.979 t - 6.683 \\
\end{split}
\end{equation}
linear maintenance model:
\begin{equation}
\begin{split}
    \hat{m}_{\mathrm{fuel}}(t, \Delta v_{\mathrm{dep}}) &= m_{\mathrm{fuel},0} - m_{\mathrm{acq}}(\Delta v_{\mathrm{dep}}) - m_{\mathrm{maint}}(t)\\
    &= 635 - 18.782 \Delta v_{\mathrm{dep}} + 2.557 - 1.536 t - 8.471 \\
    &= -1.536 t - 18.782 \Delta v_{\mathrm{dep}} + 629.086
\end{split}
\end{equation}
for $t \geq t_acq$
* Figure \ref{fig:predicted_lifetime} show this linear model applied to the same differential deplyment speed cases as considered in the deplyment sensitivity campaign. 
* Figure \ref{predicted_vs_simulated_remaining_fuel_comparison} shows zoomed in versions of the same figure to better visualize how the predictions compare against the simulated fuel consumption from the deplyment sensitivity campaign. 
\end{comment}















\begin{comment}
This builds heavily on how "Formation Acquisition" is defined in the methodology

Data analysis that needs to be performed:

01. For each run, compute $t_{acq}$ and $m_{acq} = m_{fuel}(0) - m_{fuel}(t_{acq})$

02. For each run, compute the accumulated fuel consumption. This will enable other analyses and make comparisons simpler and more intuitive

Results that will be presented:


CORE PLOTS FOR THE DEPLOYMENT SENSITIVITY RESULT

1. Follower fuel consumption from all runs in one plot
    x-axis: time [h]
    y-axis: cumulative follower fuel consumed [g]
    one curve per deployment speed case
    vertical marker at t_acq for each run, if acquired

2. Accumulated acquisition fuel consumption as a function of differential deployment speed
    x-axis: differential deployment speed magnitude [m/s]
    y-axis: acquisition fuel consumption [g]

3. Acquisition time as a function of differential deployment speed
    x-axis: differential deployment speed magnitude [m/s]
    y-axis: acquisition time [h]
4.1 RTN leader-follower relative position 3D trajectory for a selected set of runs
    RTN components in space
    choose subset of runs (0, 2, 4 diff speed)
4.2 RTN leader-follower relative positions decomposed for a selected set of runs
    choose subset of runs (0, 2, 4 diff speed)
    x-axis: time [h]
    y-axis: R, T, N components [m]
    horizontal lines for in-formation R, T, N upper/lower limit




SUPPORTING PLOTS FOR BURN Behavior 

5. Dual-bar plot showing acquisition accumulated thrust on-time and number of burns for each differential deployment speed
    x-axis: differential deployment speed magnitude [m/s]
    y-axis (left, corresponding to left vertical bar): acquisition accumulated thruster on-time / avg. thruster on-time
    y-axis (right, corresponding to right vertical bar): number of burns completed during acquisition


FEASIBILITY CHECKS

6. Operational mode timeline for all deployment cases / Another option is to show table with accumulated time spent in each mode for each run
    x-axis: time [h]
    y-axis: operational mode for each run
    show vertical lines for each run t_{acq}

7. Table summarizing some feasibility/accuracy metrics for each run:
    * The average attitude tracking error during burns [deg]
    * The accumulated time when burns were not possible (because of emergency mode)


SUMMARY TABLE

8. Show the following fields for every run:
    * Leader/follower deployment speed [m/s]
    * Differential speed [m/s]
    * Acquired?
    * Acquisition time [h]
    * Acquisition fuel [g]
    * Burns before acquisition
    * Avg. thruster on-time
    * Max RTN error [m]
    * Avg. attitude tracking error during burn
    * Min battery [%]

\end{comment}


