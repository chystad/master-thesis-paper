\section{Reference Frames}

A reference frame $\mathcal{F}^{rf}: \left\{ \mathcal{O}_{rf}; \; \hat{\textbf{x}}_{rf}, \hat{\textbf{y}}_{rf}, \hat{\textbf{z}}_{rf} \right\}$ is uniquely defined by three dextral orthogonal unit vectors 
$\{\hat{\textbf{x}}_{rf}, \hat{\textbf{y}}_{rf}, \hat{\textbf{z}}_{rf}\}$ and the origin placement $\mathcal{O}_{rf}$ \cite{grotte2020spacecraft}. This section will present the definitions for the most important reference frames used in the thesis. The reader should note that the individual reference frame subscripts are assigned to be consistent with the conventional Basilisk notation. 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{Figures/03Theory/frames.png}
    \caption{Illustration of the ECI ($\mathcal{F}^n$)and RTN ($\mathcal{F}^f$) reference frame definitions. The basis unit vectors are displayed with different lengths for illustrative purposes. Adopted from \cite{eci2014, Metz2020CollisionAvoidance} and modified from \cite{hystad2025}}.
    \label{fig:frames} 
\end{figure}

\subsection{J2000 Earth-Centered Inertial (ECI) Frame}
The \gls{eci} reference frame, $\mathcal{F}^{n}: \left\{ \mathcal{O}_{n}; \; \hat{\textbf{x}}_{n}, \hat{\textbf{y}}_{n}, \hat{\textbf{z}}_{n} \right\}$, is fixed relative to the distant stars, and is therefore commonly approximated as an inertial frame in which Newtonian mechanics are valid \cite{modsim2002}. The frame is defined with its origin in the Earth's \gls{cm}, with $\hat{\textbf{z}}_{n}$ pointing along its rotational axis towards the geographical North Pole, $\hat{\textbf{x}}_{n}$ pointing towards the vernal equinox through the equator, and $\hat{\textbf{y}}_{n}$ defined according to the right-hand rule. Multiple variants of the \gls{eci} definition exists, primarily varying in the epoch used to define the Earth's equatorial plane and equinox direction. In this thesis, the J2000 \gls{eci} frame is adopted. It is defined using the J2000.0 reference epoch, equivalent to 1st January 2000, at 12:00 \gls{tt} \cite{nasa_eci}. The frame definition is illustrated in Figure \ref{fig:frames}. 



\subsection{Earth-Centered Earth-Fixed (ECEF) Frame}
The \gls{ecef} reference frame $\mathcal{F}^{e}: \left\{ \mathcal{O}_e; \; \hat{\textbf{x}}_{e}, \hat{\textbf{y}}_{e}, \hat{\textbf{z}}_{e} \right\}$ is fixed relative to the Earth's surface, with the frame origin positioned in its \gls{cm}. This thesis adopts the WGS84 \gls{ecef} formulation as presented by Stryjewski in \cite{Stryjewski2020}, where the $\hat{\textbf{x}}_e$ axis points through the Prime Meridian in the equatorial plane, $\hat{\textbf{z}}_e = \hat{\textbf{z}}_n$ points along the Earth's rotational axis, and $\hat{\textbf{y}}_e$ is defined according to the right-hand rule. Because of the Earth's angular velocity, the transformation $\textbf{R}^e_n$ between \gls{eci} to \gls{ecef} can be expressed using a time-dependent rotation angle $\theta(t)$ about the shared $\hat{\textbf{z}}_e$ axis \cite{hystad2025}
\begin{equation}
    \textbf{R}^e_n = \begin{bmatrix}
        \cos \theta(t) & -\sin \theta(t) &  0 \\
        \sin \theta(t) & \cos \theta(t) & 0 \\
        0 & 0 & 1
    \end{bmatrix}, \qquad \textbf{R}^n_e = (\textbf{R}^e_i)^T
\label{eq:eci2ecef}
\end{equation}

\subsection{Radial-Transverse-Normal (RTN) Frame}
The \gls{rtn} reference frame, $\mathcal{F}^{f}: \left\{ \mathcal{O}_{f}; \; \hat{\textbf{x}}_{f}, \hat{\textbf{y}}_{f}, \hat{\textbf{z}}_{f} \right\}$, is a local orbital frame that is useful for expressing relative motion between spaceborne objects. Vallado presents the frame as the \textit{Satellite coordinate system, RSW} in \cite{vallado2001fundamentals}, but the \textit{RTN} name is chosen for this thesis to better convey each component's physical significance. For use in formation flight, the coordinate frame origin is assigned to the leader satellite, $l$'s \gls{cm}. The dextral orthogonal unit vectors are defined with the $\hat{\textbf{x}}_f$ axis pointing in the radial direction along $\textbf{r}^n_{l/n}$, the $\hat{\textbf{z}}_f$ axis points normal to the orbital plane, and $\hat{\textbf{y}}_f$ fulfills the right-hand rule. Note that the latter will always point in direction of positive velocity, but will only be perpendicular to the velocity for circular orbits \cite{vallado2001fundamentals, stratton2025rtn}. Using this definition, the frame can be expressed mathematically as:
\begin{equation}
    \hat{\textbf{R}}^n = \hat{\textbf{x}}_f^n = \frac{\textbf{r}^n_{l/n}}{||\textbf{r}^n_{l/n}||}, \qquad \hat{\textbf{N}}^n = \hat{\textbf{z}}_f^n = \frac{\textbf{r}^n_{l/n} \times \textbf{v}^n_{l/n}}{||\textbf{r}^n_{l/n} \times \textbf{v}^n_{l/n}||}, \qquad \hat{\textbf{T}}^n = \hat{\textbf{y}}_f^n = \hat{\textbf{z}}^n_f \times \hat{\textbf{x}}_f^n
\end{equation}
Here, $\textbf{r}^n_{l/n}$ and $\textbf{v}^n_{l/n}$ are used to denote the Earth-relative leader position and velocity expressed in \gls{eci}, while $\hat{\textbf{y}}_f$ and $\hat{\textbf{z}}_f$ correspond to the along- and cross-track directions, respectively. An illustration of the leader satellite's position relative to $\mathcal{O}_n$ and its affixed \gls{rtn} frame is shown in Figure \ref{fig:frames}.

% Add coordinate transformation RTN <-> ECI if needed.


\subsection{Body-Fixed Frame}
The body-fixed reference frame, $\mathcal{F}^{b}: \left\{ \mathcal{O}_b; \; \hat{\textbf{x}}_{b}, \hat{\textbf{y}}_{b}, \hat{\textbf{z}}_{b} \right\}$, is a moving reference frame that is rigidly attached to the spacecraft body. Because of this property, the body-fixed frame is used for expressing satellite attitude relative to an inertial frame, actuator torques and sensor measurements, since these quantities are naturally expressed relative to the body itself. The orientation of its axes are typically chosen to best represent the spacecraft geometry, and to coincide with the principal axes of inertia \cite{fossen2021}. Consequentially, CubeSat body axes are commonly selected to be perpendicular to its side panels. The frame origin is commonly placed at the \gls{cm} in order to simplify the rotational equations of motion, and to reduce translational-rotational coupling \cite{schaub2018analytical}. An illustration of the body frame definition for a 1U CubeSat is shown in Figure \ref{fig:body_rtn}. 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{Figures/03Theory/Body_RTN.png}
    \caption{Illustration of the body-fixed ($\mathcal{F}^b$)and RTN ($\mathcal{F}^f$) reference frame definitions for a 1U CubeSat. Adopted from \cite{montenbruck2015gnss}.}
    \label{fig:body_rtn} 
\end{figure}

% \subsection{Desired Attitude Frame}
% $\mathcal{F}^{r}: \left\{ \mathcal{O}_f; \; \hat{\textbf{x}}_{r}, \hat{\textbf{y}}_{r}, \hat{\textbf{z}}_{r} \right\}$




\section{Modified Rodrigues parameters}
\gls{mrp}s are 3-element attitude parameterization commonly used for spacecraft attitude control. The representation can be derived from the Euler-Rodrigues symmetric parameters, better known as the unit quaternion:
\begin{equation}
    \textbf{q} = \begin{bmatrix}
        \boldsymbol{\epsilon} \\
        \eta
    \end{bmatrix} = \begin{bmatrix}
        \epsilon_1 & \epsilon_2 & \epsilon_3 & \eta
    \end{bmatrix}^T,
    \qquad
    \textbf{q}^T \textbf{q} = |\boldsymbol{\epsilon}|^2 + \eta^2 = 1
\end{equation}

where $\eta$ is the scalar component and $\boldsymbol{\epsilon}$ is the imaginary vector. Using this definition, Shuster writes in \cite{shuster1993survey} that the \gls{mrp}s are obtained through a stereographic projection of the unit quaternion onto $\mathbb{R}^{3}$

\begin{equation}
    \textbf{p} = \frac{\boldsymbol{\epsilon}}{1 + \eta}, \qquad 
    \textbf{m} = \frac{\boldsymbol{\epsilon}}{1 - \eta}
\end{equation}

Here, $\textbf{p}$ and $\textbf{m}$ are the positive and negative forms, differing only in the singularity placement. This thesis utilizes the positive form, and will hereby be referred to as $\boldsymbol{\sigma}$. Using the axis-angle representation of a rotation, where $\hat{\textbf{n}}$ represents the unit rotation axis and $\theta$ is the angle of rotation about the axis, the positive-form \gls{mrp}s can be expressed as:
\begin{equation}
    \textbf{q} = \begin{bmatrix}
        \hat{\textbf{n}}\sin(\theta/2) \\
        \cos(\theta/2)         
    \end{bmatrix} \quad \Rightarrow \quad \boldsymbol{\sigma}(\theta, \hat{\textbf{n}}) = \hat{\textbf{n}}\tan\left(\frac{\theta}{4}\right),
\end{equation}
From this formulation, it follows that the \gls{mrp}s become singular as $\theta \rightarrow 2\pi$, since
\begin{equation}
    \tan \left( \frac{\theta}{4}\right) \rightarrow \infty \quad \text{for} \quad \theta \rightarrow 2\pi 
\end{equation}

To avoid numerical issues near the singularity, an equivalent \gls{mrp} shadow set, $\boldsymbol{\sigma}^s$, may be used \cite{okeefe2014shadow}. The shadow set transformation is defined as:

\begin{equation}
    \boldsymbol{\sigma}^s = -\frac{\boldsymbol{\sigma}}{\boldsymbol{\sigma}^T \boldsymbol{\sigma}}
\end{equation}
Switching to the shadow set when $||\boldsymbol{\sigma}|| > 1$ effectively bounds the \gls{mrp} set by the unit sphere, and prevents the parameterization from approaching the singularity numerically \cite{marandi1987preferred, okeefe2014shadow, shuster1993survey}.

% If time, point out som up- and down-sides for this attitude parameterization


\section{Classic Orbital elements}
The classic orbital elements are a set of six parameters used to uniquely define the shape, size and orientation of an orbital plane, as well as the position of an orbiting body. In astrodynamics, these parameters provide a compact and intuitive representation of orbital motion, commonly used for orbital analyses and spacecraft guidance. The classic orbital elements used in this thesis are:
% \begin{itemize}[noitemsep, topsep=0pt]
%     \item[\parbox{0.4cm}{$a$:}] semimajor axis
%     \item[\parbox{0.4cm}{$e$:}] eccentricity
%     \item[\parbox{0.4cm}{$i$:}] inclination
%     \item[\parbox{0.4cm}{$\Omega$:}] right ascension of the ascending node (RAAN)
%     \item[\parbox{0.4cm}{$\omega$:}] argument of perigee 
%     \item[\parbox{0.4cm}{$M$:}] mean anomaly
% \end{itemize}
\[
\begin{array}{@{}l@{\quad}c@{\quad}l@{}}

\begin{array}{@{}r@{\;}l@{\quad}l@{}}
a      &: & \text{semimajor axis}\\
e      &: & \text{eccentricity}
\end{array}
&
\left.\vphantom{\begin{array}{c}x\\x\end{array}}\right\}
&
\text{orbital plane size and shape}
\\
\begin{array}{@{}r@{\;}l@{\quad}l@{}}
i      &: & \text{inclination}\\
\Omega &: & \text{right ascension of the ascending node (RAAN)}\\
\omega &: & \text{argument of perigee}
\end{array}
&
\left.\vphantom{\begin{array}{c}x\\x\\x\end{array}}\right\}
&
\text{orbital plane orientation}
\\[1em]
\begin{array}{@{}r@{\;}l@{\quad}l@{}}
M      &: & \text{mean anomaly}
\end{array}
&
\left.\vphantom{\begin{array}{c}x\end{array}}\right\}
&
\text{orbital body position}

\end{array}
\]
It is important to note that the semimajor axis, and mean anomaly are in some orbital element configurations replaced with the angular momentum $h$, and the true anomaly $\theta$, respectively. In any case, these six parameters define a Keplerian orbit, and they can be grouped according to the orbital property they describe: The semimajor axis and eccentricity define the shape and size of the orbital plane, while the inclination, RAAN and argument of perigee describe the plane's orientation. Finally, the mean anomaly is used to describe the spaceborne object's position along the orbital trajectory. An illustration of the classic orbital elements is shown in Figure \ref{fig:OEs}. 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\textwidth]{Figures/03Theory/OEs.png}
    \caption{Orbital elements relative to the \gls{eci} frame. Courtesy of \cite{curtis2005orbital}}.
    \label{fig:OEs}
\end{figure} 

The semimajor axis is defined as half of the longest ellipse diameter, and determines the overall size of the orbit. For elliptical orbits, this parameter is directly coupled with the orbital period through Kepler's third law. The eccentricity is a measure of how much the orbital trajectory deviates from a circle. It takes values in the span between zero and one, where an eccentricity of zero corresponds to a perfectly circular orbit, and an increasing eccentricity correspond to an elliptical orbit of increasing elongation. 

The inclination is the intersection angle between the orbital plane and the reference plane, commonly the equatorial plane spanned by the $\hat{x}_n$ and $\hat{y}_n$ axes in \gls{eci}. This angle determines the tilt of the orbital plane, while its orientation about the $\hat{z}_n$ axis is determined by the RAAN parameter. For nonzero inclinations, the orbital trajectory intersects the reference plane at two points, the ascending and descending nodes. The ascending node is where the object travels from the Southern to the Northern Hemisphere, and RAAN is defined as the angle between this node and the $\hat{x}_n$ axis in the equatorial plane. 

The argument of perigee specifies the orientation of the ellipse within the orbital plane. It is defined as the angle from the ascending node to the perigee along the orbital trajectory, where perigee is a term specific to Earth satellites, describing the point along the trajectory closest to Earth. Finally, the mean anomaly defines the orbiting body's position along the orbit as a function of time. It is defined as the elapsed fraction of the orbital period since the orbiting body passed the perigee \cite{curtis2005orbital, astronomicalreturns_orbitalelements}. 


 
\newpage
\section{Orbital perturbations}
Perhaps note that I make use of the orbital perturbation review conducted in \cite{hystad2025}??

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{Figures/03Theory/perturbation magnitudes.png}
    \caption{Acceleration magnitudes caused by various orbital perturbations. Courtesy of \cite{montenbruck2000}}.
    \label{fig:orbital_perturb}
\end{figure} 


\subsection{Spherical harmonics}
Keplerian motion assumes that the Earth can be represented as a perfectly spherical body with all its mass concentrated in the earth-centered inertial frame origin. Under this assumption, the gravitational force acting on a spacecraft, $s$, can be modeled using Newton's law of gravity unsing the notation from \cite{hystad2025}:
\begin{equation}
    \textbf{a}_{grav} = \left(\ddot{\textbf{r}}_{s/n}\right)_{grav} = - \frac{\mu}{||\textbf{r}_{s/n}||_2^3} \textbf{r}_{s/n} \iff \left(\ddot{\textbf{r}}_{s/n}\right)_{grav} = \nabla U, \quad \text{where } U = \mu\frac{1}{||\textbf{r}_{s/n}||_2}
\label{eq:newton_gravity}
\end{equation}
Here, $\mu = GM$ is the Earth's gravitational constant, and $\nabla U$ is the gradient of the gravity potential. In accordance with the spherical assumption, the gravity potential is only dependent on the satellite's geocentric distance, implying uniform density. In actuality, the Earth's mass is unhomogeneously distributed through its volume, and centrifugal forces causes the planet to bulge outwards around the equator due to its angular velocity. These irregularities produce perturbing accelerations that gradually alter the orbital trajectories over time. 

Montenbruck \& Gill explain that these deviations from spherical symmetry can be modeled by expanding the Earth's gravity potential into a spherical harmonic series representation \cite{montenbruck2000}. The resulting series expresses the geopotential as a weighted sum of spherical harmonic terms describing gravitational irregularities caused by the Earth's unhomogeneous mass distribution. Here, the spacecraft position is represented in \gls{ecef} spherical coordinates consisting of the geocentric distance $r = ||\textbf{r}^e_{s/n}||_2$, the geocentric latitude $\phi^e$, and the longitude $\lambda^e$. Using these coordinates together with the associated Legendre polynomials, the geopotential may be written in the general spherical harmonic form as
\begin{equation}
U(r, \phi^e, \lambda^e) = \frac{\mu}{R} \sum_{n=0}^{\infty} \sum_{m=0}^{n} \left(C_{nm}V_{nm} + S_{nm}W_{nm}\right)
\label{eq:general_spherical_harmonic}
\end{equation}
where, 
\begin{equation}
V_{nm} = \left(\frac{R}{r}\right)^{n+1} P_{nm}(\sin\phi^e)\cos(m\lambda^e),
\qquad
W_{nm} = \left(\frac{R}{r}\right)^{n+1} P_{nm}(\sin\phi^e)\sin(m\lambda^e).
\label{eq:spherical_harmonics}
\end{equation}
Here, $R$ denotes the Earth's radius, $P_{nm}$ are the associated Legendre polynomials, while $C_{nm}$ and $S_{nm}$ are the normalized geopotential coefficients that encode the Earth's non-uniform mass distribution. The $_{nm}$ subscript denotes the degree $n$ and order $m$, where increasing value correspond to decreasingly significant perturbation terms. The harmonic degree relates to longitudinal mass variation, while the order determines the longitudinal dependency on the harmonic term. 

The reader should note that a common simplification is to assume order $m=0$ due to comparatively small variations in the Earth's gravity field along the longitude. This yields the zonal harmonic expression, where the $S_{n0}$ terms vanish, and the geopotential coefficients are commonly expressed as $J_n = -C_{n0}$. This gives the following expression derived from \cite{montenbruck2000, vallado2001fundamentals}:
\begin{equation}
    U(r, \phi^e) = \frac{\mu}{r} \left[ 1 - \sum_{n=2}^{4} J_n \left( \frac{R}{r} \right)^n P_n(\sin \phi^e) \right]
\end{equation}
The resulting zonal model captures the dominant secular perturbation effects while significantly reducing the complexity of the gravity model. Amongst the zonal coefficients, the second degree term $J_2$ dominates the non-spherical effects, as is clearly evident by the substantially larger acceleration magnitude of $J_{2,0}$ in Figure \ref{fig:orbital_perturb} compared to the other terms. The Figure also illustrates that the higher degree and order terms rapidly decrease in magnitude. However, to include the dominant lateral and longitudinal non-spherical effect, this thesis employs the general spherical harmonic formulation in Equation \ref{eq:general_spherical_harmonic}, with truncated degree and order.


    \subsection{Atmospheric drag}

        \subsection{NRLMSISE-00 (MSIS) Atmosphere model}

    \subsection{Solar radiation pressure}
Maybe include the eclipse model here?

    \subsection{Third-Body Perturbations}

    \subsection{Relative significance in LEO}





\section{Formation flying dynamics}
% Check this source sent from Tommy: https://link.springer.com/article/10.1007/s40295-025-00556-w


\section{Differential perturbation effects}


\section{Spacecraft subsystems}

    \subsection{Reaction Wheels}

    \subsection{Cold gas thrusters}

    \subsection{Solar panels}

    \subsection{Communication antenna}


\section{GNSS-R operations}

    \subsection{Pointing requirements}

    \subsection{Formation constraints}


\section{Simplified General Perturbations 4 (SGP4)}