\begin{comment}
The introduction to this "Comparative Propagation Simulator" chapter should be written here. It should be structured like this in chronological order:
1. Quickly present the motivation for using this simulator. Mention that this simulator is a continuation of the comparative propagator used in my specialization project in \cite{hystad2025}, and that this continuation was necessary because of 2 reasons:
    * Most importantly because the Skyfield SGP4 used as a comparison baseline was also subject to prediction inaccuracies. (we know this because TLE propagation is known for being efficient, but diverges only after ~2 days)
    * Secondly, because the exponential atmosphere model used in this project was too simplistic and wrongly tuned, which led to a much smaller drag then expected for the tested orbital altitude
2. State that Comparative Propagation Simulator developed for this thesis aims to rectify these mistakes. 
3. Explain what this chapter will present the high-level structure of this simulator and provide a deeper understanding of how Skyfield and Basilisk are configured to propagate the translational states. 
4. For future reference, state that the term "simulator" will be used to denote the Comparative Propagation Simulator in its entirety, while "sub-simulation" will be used when referencing the individual propagators within it (Skyfield or Basilisk)
\end{comment}


The Comparative Propagation Simulator developed in this thesis is a continuation of the comparative propagation framework from the specialization project \cite{hystad2025}. In that work, a Basilisk numerical propagator was compared against a Skyfield SGP4 propagation baseline to assess the impact of individual perturbation effects and numerical integrator configuration to evaluate Basilisk long-horizon prediction accuracy. However, two important limitations motivated further development of the simulator. First, the Skyfield SGP4 baseline was itself subject to prediction inaccuracies, which made it a weak reference for assessing long-horizon prediction accuracy. \gls{tle}-based propagation, such as SGP4, is primarily intended for efficient near-epoch orbit prediction, and is known to diverge over longer propagation intervals. Second, the Basilisk exponential atmospheric model used in the specialization project was too simple and wrongly tuned, which resulted in significantly smaller drag accelerations than expected.

The Comparative Propagation Simulator developed for this thesis aims to address these limitations by extending both the Skyfield and Basilisk parts of the comparison. The following sections present the implementation of the simulator at a high level of abstraction to provide an understanding of how the separate propagators are initialized, and how the output data is generated and made comparable. First, the overall simulator architecture is introduced, including the configuration system, data flow, execution sequence, and output files. Then the simulator input parameters will be presented in further detail. Specific parameters or simulation configuration is outside the scope of this chapter, but are presented in \ref{chap:shared}. For future reference, the term \textit{simulator} will be used to address the Comparative Propagation Simulator in its entirety, while \textit{sub-simulation} will be used to denote the individual Skyfield or Basilisk propagators. 






\newpage
\section{Simulation Architecture}
\label{sec:propagator_architecture}
The overall structure and execution order of the Comparative Propagation Simulator can be represented as a sequence of three main program blocks: initialization of the simulation environment, Skyfield SGP4 propagation, and Basilisk numerical propagation. This execution order is necessary because the Basilisk sub-simulation is initialized using the Skyfield-propagated spacecraft states at the simulation start time. A more detailed overview of the simulator architecture is shown in Figure \ref{fig:propagator_comp_flow}. The figure illustrates the execution sequence from left to right, as well as the abstracted program flow within each program block. The vertically stacked layers separate file inputs, internal object instances, program blocks and output files. The top layer contains the user-defined input files, including configuration files, \gls{tle} data, and external model data. The middle layer shows the main program blocks, while the adjacent layers show how object instances and data are created and passed between blocks. Finally, the bottom layer shows the output files generated by the simulator.
\begin{figure}[H]
    \centering
    \includegraphics[width=1.0\textwidth]{Figures/04Method/Propagation Accuracy Comparison Simulator Flowchart v2.png}
    \caption{Detailed Comparative Propagation Simulator flow diagram. Adopted from \cite{hystad2025}. A full-size version of this diagram is shown in the Appendix Figure \ref{fig:full_size_formfly_flow}}
    \label{fig:propagator_comp_flow} 
\end{figure}
\subsection{Initialization of Propagator Simulation Environment}
\label{sec:propagator_init}
When the simulator is executed, the simulation environment is initialized by loading the configuration files and the leader \gls{tle} time series. The file \texttt{default.yaml} defines the parameters shared by both propagation methods, while \texttt{skyfield.yaml} and \texttt{basilisk.yaml} define settings specific to the Skyfield and Basilisk sub-simulations, respectively. The file \texttt{leader\_tle\_series.txt} contains a sequence of \gls{tle}s for the leader spacecraft, ordered by epoch, spanning the full simulation duration with a maximum epoch separation of 12 hours. The use of this \gls{tle} time series is described in more detail in Section \ref{sec:sequential_tle_methodology}. For traceability, a timestamped snapshot of the combined simulation configuration is written to file, allowing the exact configuration used to generate a given set of outputs to be inspected later. The validated configuration parameters are then collected into a shared configuration instance that is used by the subsequent Skyfield and Basilisk program blocks.


\subsection{Skyfield SGP4 Propagator Flow}
The Skyfield SGP4 sub-simulation is initialized after the shared configuration instance has been created. During this step, the leader \gls{tle} time series is loaded and used to construct the corresponding follower \gls{tle} time series with constant shifted mean anomaly relative to the leader. These are then used to create Skyfield satellite objects, which define the SGP4 propagation model used for each spacecraft. Finally, the simulator pre-determines which specific \gls{tle} from the time series should be used at any given simulation time. This produces a time-indexed mapping between simulation time and \gls{tle} set, allowing the subsequent Skyfield propagation block to evaluate each spacecraft state using the \gls{tle} closest to the requested propagation time.


After the Skyfield propagator has been initialized, the Skyfield SGP4 sub-simulation is executed for the full simulation duration. At each simulation time step, the simulator selects the pre-determined \gls{tle} for each spacecraft, and evaluates the corresponding SGP4 model to obtain the spacecraft position and velocity in the \gls{eci} frame. This produces a time series of translational states for both the leader and follower spacecraft, where the propagation remains tied to the continuously updated \gls{tle} series rather than a single fixed \gls{tle} epoch. When the Skyfield propagation is complete, the states at the initial simulation time are extracted, and the resulting state time series are written to a timestamped output file, \texttt{<time>\_skf.h5}. This output represents the Skyfield comparison baseline used in the later comparison against the Basilisk numerical propagation.


\subsection{Basilisk Propagator Flow} 
\label{sec:basilisk_propagator_flow}
Once the Skyfield SGP4 sub-simulation has completed, the Basilisk propagator is initialized. The initialization first creates the Basilisk simulation process and task, which are required by the Basilisk framework to schedule and integrate all attached simulation models. The environmental and spacecraft dynamics models are then created according to the selected simulation configuration. In contrast to the Skyfield sub-simulation, where the propagation behavior is determined by the selected \gls{tle}s and the SGP4 model, the Basilisk sub-simulation allows different combinations of explicitly modeled perturbation effects to be enabled or disabled. This configurability is indicated by the \textit{(if config)} annotations in Figure \ref{fig:propagator_comp_flow}, and makes it possible to run otherwise identical propagation cases with different environmental model combinations. Finally, the leader and follower spacecraft are initialized and the numerical integration method is configured.

After initialization, the Basilisk numerical propagation is run for the entire simulation duration. During this process, Basilisk integrates the spacecraft equations of motion using the configured numerical integration method and the environmental and dynamics models attached during initialization. When the propagation is complete, the translational states of each spacecraft is written to a timestamped output file, \texttt{<time>\_bsk.h5}. This completes the Comparative Propagation Simulator execution and provides the Basilisk state time seris used for comparison against the Skyfield SGP4 baseline.

\begin{comment}
This section should include the following:
* First paragraph:
    - Explain that the simulator structure can be represented as a series of sequential program blocks: 
        1. configuration/initialization of simulation environment
        2. Skyfield SGP4 propagation
        3. Basilisk propagation
    - Quickly note that this ordering is necessary because the Basilisk sub-simulation is dependent on the Skyfield SGP4 output for its initial conditions. 
    - Explain that a more detailed overview of the program blocks is illustrated in Figure \ref{fig:propagator_comp_flow}. Describe this figure in more detail, stating that it shows the program block execution order from left to right, as well as the abstracted program flow within each block (don't go into details, simply state this factoid). Also explain the vertically stacked "layers" illustrated in this figure:
        * Top layer represents the file-input layer ("User input" in diagram). This is where all external user-defined config files and model parameter files belong.
        * Bottom layer represents the file-output layer ("Output files" in diagram). The simulation output data, generated \gls{tle} files and config snapshot belong in this layer. 
        * Middle layer shows the program blocks and the abstracted program flow within them
        * The top- and bottom-adjacent layers show how object instances are defined and passed between program blocks. This is essentially how the blocks transfer information between them

* Second paragraph:
    - This paragraph should explain how the "Initialize simulation environment" program block works. 
    - After the simulator is executed, the config and leader \gls{tle} file are loaded into the simulation environment. \texttt{default.yaml} define all the configuration parameters shared by both Skyfield and Baskilisk, while \texttt{skyfield.yaml} and \texttt{basilisk.yaml} contain parameters unique to the individual sub-simulation. \texttt{leader_tle_series.txt} contain a series of sequential \gls{tle} files for the leader spacecraft ordered by epoch, and at least spanning the entire simulation duration with a maximum epoch separation of 12 hours. More information about this in Section \ref{sec:sequential_tle_methodology}. 
    - For traceability, a snapshot of the simulation configuration is exported with a timestamp, enabling later inspection of configuration used to produce a set of simulation outputs. 
    - All config parameters are then validated, and used to create a config instance.

* Forth paragraph:
    - This paragraph should explain how the "Initialize Basilisk Propagator" program block works. it should not focus on the individual environmental or dynamics models, but rather note that it is possible to run any number of perturbation effect combinations by changing the simulation configuration
    - Start by stating that the basilisk propagator is first initialized by setting up the simulation task and process. Very briefly comment that this is required by the Basilisk framework to integrate all the attached models. 
    - State that the environmental and dynamics models are created. Quickly comment on the fact that any combination of orbital acting perturbations can be selected by running different simulation configurations. This is denoted by "(if config)" in Figure \ref{fig:propagator_comp_flow}.

* Fifth paragraph:
    - This paragraph should explain how the "Run Basilisk Propagator" program block flow works. 
    - The Basilisk numerical integrator runs for the entire simulation duration using the configured numerical integration method
    - After the Basilisk propagator is complete, the translational states are outputted to \texttt{<time>.bsk.h5}
\end{comment}


\section{Simulator Inputs}
As mentioned in Section \ref{sec:propagator_init}, each simulation case is defined through three user-defined configuration files, which have been summarized in Table \ref{tab:propagator_config_files}. This structure makes it easy to keep the simulated scenario fixed while independently changing the configuration of each propagation method. The file \texttt{default.yaml} contain all parameters required to define the simulation scenario. This includes the simulation start time and duration, the leader TLE time series, the number of satellites, the initial in-plane separation between the leader and follower, spacecraft physical parameters, and plotting options.

\begin{table}[H]
\centering
\begin{tabular}{llc}
\hline
\textbf{Config file} & \textbf{Input parameters} & \textbf{Unit} \\ \hline
default.yaml
& \begin{tabular}[c]{@{}l@{}}Simulation start time\\ Simulation duration\\ Leader TLE \\ In-plane separation angle \\ Number of satellites \\ For each satellite:\\
\quad Mass\\
\quad Drag coefficient\\
\quad Cross-section area perpendicular to velocity\\
\quad Radiation pressure coefficient\\
\quad Cross-section area perpendicular to the Sun-vector\\
Data processing and plotting flags\end{tabular}
& \begin{tabular}[c]{@{}c@{}}UTC\\ h\\ -\\ deg\\ -\\ -\\ kg\\ -\\ m$^2$\\ -\\ m$^2$\\ -\end{tabular} \\ \hline
skyfield.yaml
& SGP4 step size
& sec \\ \hline
basilisk.yaml
& \begin{tabular}[c]{@{}l@{}}Basilisk step size\\ Numeric integrator\\ Spherical harmonics degree\\ Enable spherical harmonics flag\\ Enable exponential density drag flag \\ Enable MSIS drag flag \\ Enable solar radiation pressure flag\\ Enable Sun third-body pull flag\\ Enable Moon third-body pull flag\end{tabular}
& \begin{tabular}[c]{@{}c@{}}sec\\ -\\ -\\ -\\ -\\ -\\ -\\ -\\ -\end{tabular} \\ \hline
\end{tabular}
\caption{A complete list of all Comparative Propagation Simulator configuration input parameters. Adopted from \cite{hystad2025}}
\label{tab:propagator_config_files}
\end{table}

The file \texttt{skyfield.yaml} contains the Skyfield-specific propagation setting, which is only the time step between each SGP4 evaluation. Finally, the \texttt{basilisk.yaml} file defines the Basilisk propagation setup, including the numerical integrator, integration step size, spherical harmonics degree, and boolean flags for enabling or disabling individual perturbation models. In addition to the configuration files, the Basilisk sub-simulation uses external model data for selected environmental models. This includes the gravity coefficient file used by the spherical harmonics model and the space-weather data used by the NRLMSISE-00 atmosphere model.







% \section{(Not essential IF adequately described in architecture sction) Skyfield SGP4 Reference Methodology}
% \label{sec:sequential_tle_methodology}
% \begin{comment}
% This section should present:
% * TLE handling / follower generation
% * Sequential TLE usage
% * Why this is useful as a propagation comparison baseline
% \end{comment}



% This chapter presents the structure and implementation of the Comparative Propagation Simulator. First, the overall simulation architecture is described, including how configuration files, TLE data, environmental model data, and output files are handled. The Skyfield SGP4 reference methodology is then presented, with emphasis on follower TLE generation and sequential TLE usage. Finally, the initialization and execution of the Basilisk numerical propagation are described, including how the Skyfield-derived initial states are used to ensure that both propagation methods start from the same translational state. For clarity, the term simulator is used to refer to the complete Comparative Propagation Simulator, while sub-simulation denotes one of the individual propagation branches within it, namely the Skyfield SGP4 sub-simulation or the Basilisk numerical sub-simulation.


% Instead of relying on a single \gls{tle} propagated over the full simulation interval, the Skyfield SGP4 comparison baseline now uses a time series of continuously updated \gls{tle}s, allowing the comparison baseline to remain closer to observation-informed orbital estimates throughout the simulation. In Basilisk, the atmospheric modelling is also improved by allowing the use of the NRLMSISE-00 atmosphere model driven by external space-weather data, replacing the overly simplified drag representation used previously.





